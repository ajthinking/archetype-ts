# TypeSmith | TS-manipulator | Archetype TS :dizzy:

```ts
File.create("src/types/Contact.ts").addTypeAliasFromObject(types).save();
```

```ts
File.load("src/types/Contact.ts").cursorAtRoot().sort().save();
```
